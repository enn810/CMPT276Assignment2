<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div>
        <form action="/viewMain" method="POST">
            <h2>Database Results <input type="submit" value="Return to Person Submission Page"></h2>
        </form>
    </div>

    <div>
        <table>

            <% rows.forEach(function(r) { %>
            <tr>
                <svg width=<%= r.size %> height=<%= r.height %> style = "padding: 25px">
                    <g>
                        <rect x="0" y="0" width=<%= r.size %> height=<%= r.height %> fill="black" fill-opacity="0.5"></rect>
                        <text x="0" y="50" font-family="Verdana" font-size="" fill="blue"><%= r.name %></text>
                    </g>
                </svg>

            </tr>

            <% }); %>
        </table>
    </div>


    <div>
        <form action="/viewDatabase" method="POST">
            <h2>VIEW DATABASE <input type="submit" value="View Database"></h2>
        </form>
    </div>


</body>

</html>